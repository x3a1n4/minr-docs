
# Pattern
This is the default generated description for the class Pattern. Include code examples, screenshots, ect. here.

## Constructors
| Constructor  | Description              |
| ------------ | ------------------------ |
| `Pattern Pattern(String[] blocks, String type)` | Sets a pattern with a get method Will just be a single block for now But new constructors could add cool patterns in the future |
| `Pattern Pattern(String[] blocks, Double[] weights, String type)` | No documentation |

## Methods
| Method                                                  | Description                                                                         |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `Pattern Pattern(String[] blocks, String type)` | Sets a pattern with a get method Will just be a single block for now But new constructors could add cool patterns in the future |
| `Pattern Pattern(String[] blocks, Double[] weights, String type)` | No documentation |
| `String get(BlockLocation targetLocation)` | No documentation |

## Fields
| Field                                                  | Description                                                                                       |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| `String[] blocks` | No documentation |
| `String type` | No documentation |
